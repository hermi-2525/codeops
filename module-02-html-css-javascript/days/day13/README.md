
## CSS concepts used
- External stylesheet
- `:root` CSS variables and `var()`
- Universal, element, class, ID and attribute-related CSS concepts
- Specificity
- Box model
- `box-sizing: border-box`
- Padding and margin
- Grid
- Typography and `rem`
- Hex, RGB, HSL and `rgba()`
- `:hover`, `:focus`, `:nth-child()`
- Responsive media query
